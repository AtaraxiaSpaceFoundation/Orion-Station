# Base Shared guidance

Root Shared is base content used across the repository. Changes here affect every module and should be reusable beyond Orion.

Keep contracts, components, events, and prediction-compatible behavior here only when they belong to base content. Do not introduce module-specific references or hidden server information.
