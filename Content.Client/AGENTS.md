# Base Client guidance

Root Client is base presentation and UI shared by the repository.

Keep client-only rendering, controls, XAML, and UI behavior here only when it belongs to base content. Do not add Orion-specific presentation when the Orion Client project can own it.
