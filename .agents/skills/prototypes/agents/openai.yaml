interface:
  display_name: "Prototypes"
  short_description: "Create, inherit, compose, localize, and validate YAML prototypes in the correct resource root."
  default_prompt: "Use $prototypes and the nearest scoped AGENTS.md for this task."
