interface:
  display_name: "Build And Packaging"
  short_description: "Work with solution structure, CI, generated content, packaging, and release outputs."
  default_prompt: "Use $build-and-packaging for project, workflow, and packaging changes."
