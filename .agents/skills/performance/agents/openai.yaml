interface:
  display_name: "Performance"
  short_description: "Review hot paths, allocations, iteration, caching, event frequency, and correctness-preserving optimization."
  default_prompt: "Use $performance and the nearest scoped AGENTS.md for this task."
