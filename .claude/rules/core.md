# Canonical guidance

Read `/AGENTS.md`, the nearest scoped `AGENTS.md`, relevant `.agents/rules`, and task-specific `.agents/skills`.

Do not reproduce those documents here. Use this adapter only to ensure Claude loads the canonical Orion guidance.
