# Server adapter

For server code, load the nearest scoped `AGENTS.md` and the relevant authority, security, persistence, interaction, and testing skills.

Revalidate every client-originated request. Keep protected outcomes, hidden information, persistence, and anti-abuse checks authoritative. Prefer owning system APIs over direct component mutation.
