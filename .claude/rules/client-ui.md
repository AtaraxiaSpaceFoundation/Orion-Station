# Client and UI adapter

For client code and XAML, load the nearest scoped `AGENTS.md` plus the appearance, XAML, BUI, EUI, audio, and prediction skills needed by the task.

Keep authority on the server, presentation on the client, and lifecycle cleanup explicit. Check duplicate subscriptions, overlays, windows, predicted effects, and localization.
